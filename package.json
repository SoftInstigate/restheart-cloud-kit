{
  "name": "restheart-cloud-kit-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w packages/kit && npm run build -w packages/kit-ng && npm run build -w packages/kit-react && npm run build -w packages/kit-vue"
  }
}
